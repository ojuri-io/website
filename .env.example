# Set to "coming-soon" to deploy the holding page with countdown +
# early-access form. Any other value (or unset) ships the full landing.
# The page also auto-switches to the full landing once LAUNCH_DATE
# (src/config/launch.ts) passes, even if this is left set.
VITE_LAUNCH_MODE=coming-soon
